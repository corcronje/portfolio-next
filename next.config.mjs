/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "d3qdc2zh3mwabb.cloudfront.net",
      "cdn.offerzen.com",
      "cdn.jsdelivr.net",
    ],
  },
};

export default nextConfig;
